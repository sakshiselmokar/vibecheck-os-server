services:
  - type: web
    name: vibecheck-os-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: HF_TOKEN
        sync: false   # Render prompts you to fill this in the dashboard